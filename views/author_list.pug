extends layout

block content
  h1= title

  ul
    each author in author_list
      li
        a(href=author.url) #{author.name}
        |  (#{author.due_back_formatted} - #{author.due_back_formatted})
    else
      li There are no authors.
